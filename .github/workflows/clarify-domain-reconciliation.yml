name: Clarify domain reconciliation

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Apply clarification
        run: python .github/scripts/clarify_domain_reconciliation.py
      - name: Remove transport and validate final delta
        run: |
          git rm -f .github/scripts/clarify_domain_reconciliation.py .github/workflows/clarify-domain-reconciliation.yml
          test "$(git diff --name-only 793c9e1c046d4b9a6b531b1b645c131af50a7bbb | sort)" = "$(printf '%s\n' \
            .agents/skills/README.md \
            .agents/skills/review-spec-remediation/SKILL.md \
            .agents/skills/review-spec/SKILL.md \
            .agents/skills/to-tickets/SKILL.md \
            .agents/skills/verify-ticket-closure/SKILL.md \
            docs/process/common-sense-invariant-hardening-semantic-domain-finality-addendum.md | sort)"
          git diff --check 793c9e1c046d4b9a6b531b1b645c131af50a7bbb
      - name: Persist clarification
        run: |
          git config user.name "Bob Taylor"
          git config user.email "bobltaylorjr@gmail.com"
          git add -A
          git commit -m "fix(workflow): clarify domain reconciliation routing"
          git push origin HEAD:main
